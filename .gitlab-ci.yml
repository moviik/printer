default:
  image: node:10.11.0
  cache:
    paths:
      - node_modules

stages:
  - build
  - test

install_dependencies:
  stage: build
  script:
    - npm install

tests:
  stage: test
  script:
    - npm run lint
    - npm test